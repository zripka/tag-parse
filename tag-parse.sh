#! /bin/bash

# if no command args are entered, print command line arg information to the terminal
if [ $# -eq 0 ]
then
  echo "Command line arguments: input filename, output filename, interview id"
fi

# format the text file with fmt to break up long lines of text originally without newline

# search the text file for text enclosed by @@ and ~~ tags

# write to new text file: line number + tagged text + (interview ID) + new line

# exit the script
exit 0